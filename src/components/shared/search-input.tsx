import React from "react";

type Props = {
  className?: string;
};

export const SearchInput = (props: Props) => {
  const { className } = props;
  return <div className={className}>inputinputinputinputinputinputinput</div>;
};
